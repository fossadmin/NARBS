# modules/home/sops.nix — SOPS module (Home Manager)
#
# Secrets management with SOPS.
# Enable with: home.manager.modules.sops.enable = true;

# NOTE: Full sops-nix integration is in NixOS module, not HM
# This is a placeholder for user-level sops config

{ ... }:

{
  # User-level sops config would go here
  # For now it's handled by sops-nix in NixOS
}